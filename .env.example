# Optional Phase 6 LLM narration (display-only). Clinical truth stays in the engine.
# Without OPENAI_API_KEY, PlayShell uses the Phase 5 deterministic story director.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
